(jbuild_version 1)

(executable (
  (name test)
  (libraries (ezpostgresql alcotest alcotest-lwt))
  (preprocess (pps (lwt.ppx)))
))

(alias (
  (name runtest)
  (deps (test.exe))
  (action (run ${<}))
))
